## What & why

<!-- one paragraph: the behavior change and the reader/developer problem it solves -->

## Checklist

- [ ] `npm test` passes and new behavior has a test
- [ ] `npm run demo` output still reads correctly (if replacement behavior changed)
- [ ] Commits are signed off (`git commit -s`, DCO)
